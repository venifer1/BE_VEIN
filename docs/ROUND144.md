# VEIN Round 144

Date: 2026-09-13

## 차트 오버레이 지표(SMA·볼린저밴드) 순수 계산 단위 테스트 (FE, 회귀 보호)

자율 루프 R144. 종목 상세 차트(`chart-view.tsx`)가 캔들 종가로 그리는 클라이언트 측
기술지표 `sma`·`bollinger`(`lib/indicators.ts`)는 순수 수치 계산인데 테스트가 없었다.
오버레이가 조용히 어긋나면 눈으로 잡기 어려우므로 회귀 보호 추가.

### 바뀐 것

- 신규 `lib/indicators.test.ts` (+8):
  - `sma`: 선행 null 정렬(창 3 → `[null,null,2,3,4]`), 표본<창 전부 null, 창≤0 전부 null,
    창 1은 원본 그대로, 빈 입력 빈 출력.
  - `bollinger`: 상수열은 sd=0 → 밴드가 중앙선으로 수렴, 모집단 표준편차로 `중앙±mult·sd`
    (창 {6,8}→평균7·sd1→상8/하6), 창 미만 선행 null.
- 코드 변경 없음(테스트만).

### 검증

FE `npx vitest run` **6파일 55개 그린**(R143 47 → +8). 작성 중 기대값 산술 오류 1건
(이동평균 대상 창 오인)을 테스트 실패로 즉시 잡아 수정 → 실측값으로 확정.
