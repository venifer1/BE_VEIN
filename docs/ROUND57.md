# VEIN Round 57

Date: 2026-09-12

## 조건검색 저장 에러 표면화 + 틱띄기 지표 자릿수 정리 (FE 폴리시)

자율 개선 루프 R57. R52/R56에서 플랜 한도 게이트(402)를 넣었는데, **조건검색식 저장 실패가
화면에 전혀 안 떴다**(save 에러 미표시) — 게이트가 조용히 실패하는 반쪽 상태였다.

### 바뀐 것 (FE)

- **조건검색식 저장 에러 표면화** (`condition-scanner-panel.tsx`): `save.isError`면 저장 버튼 아래
  에러 메시지 표시(`extractError`로 추출). FREE 한도 초과 시 402의 한글 안내("무료 플랜은
  조건검색식을 최대 3개까지... PRO로 업그레이드")가 그대로 보인다. 저장 중엔 "저장 중" 표기.
  (참고: 알림 생성 다이얼로그는 이미 `body.message`로 402를 표면화하고 있어 무변경.)
- **틱띄기 지표 자릿수 통일** (`scanner/page.tsx`): 스캘핑 랭킹의 스프레드/TPS/마이크로변동/불균형이
  `4.207842`처럼 들쭉날쭉하게 표시되던 것을 `n2()`로 **2자리 고정**(4.21).

### 검증

- 프론트 `typecheck`/`build` 통과. **Chrome smoke 0에러**. 백엔드/계약 무변경.
