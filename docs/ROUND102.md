# VEIN Round 102

Date: 2026-09-13

## 신호 상세 헤더에 종합 점수 노출 + 상태 문서 갱신 (FE+문서, R101 후속)

자율 루프 R102. R101이 카드에 "종합 {점수}"를 노출했는데, 신호 상세 헤더는 여전히 "구조
점수"만 보여 카드↔상세가 어긋났다(종합 점수는 상세 하단 Explain 패널에만). 상단에서 바로
일관되게 보이도록 정리.

### 바뀐 것

**프론트**
- 신호 상세 헤더 점수를 `HeaderScore` 컴포넌트로: 종합 Pattern Score(있으면) **"종합 {점수} ·
  구조 {점수}"**, 없으면 "구조 {점수}". explain 쿼리는 하단 Explain/Action 패널과 공유
  (React Query dedup)라 추가 요청 없음.

**문서**
- PROJECT_STATUS §2.2 Pattern Score 항목에 영속화(V29)·종합점수 정렬(R90)·카드 노출(R101)
  반영.

### 검증

`tsc`·`next build` 통과. 헤더 표시 추가(로직·API 무변경), 종합 미계산 신호는 기존과 동일.
</content>
