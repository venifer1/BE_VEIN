# VEIN Round 118

Date: 2026-09-13

## 홈 관심종목 개수 표시 + R117 레이아웃 시각 검증 (FE)

자율 루프 R118. R117로 관심종목을 상단으로 올린 뒤, 카드 헤더에 담긴 종목 수를 표시해
한눈에 규모를 알게 한다(스캐너 결과 개수와 동일 패턴). 겸해서 R117 레이아웃 변경과 R112
관심종목 등락률을 mock 스모크+홈 캡처로 시각 검증.

### 바뀐 것 (프론트 전용)

- `WatchlistMini` 헤더: 항목 있으면 `관심종목 (N)` 개수 표시.

### 검증

`tsc`·`next build` 통과. mock 빌드(3100) full smoke **0에러**, 홈 캡처로 R117 순서(주목신호→
검색→관심종목→시장개요)·정상 렌더 확인. 실데이터 빌드로 원복.
</content>
