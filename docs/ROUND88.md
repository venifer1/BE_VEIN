# VEIN Round 88

Date: 2026-09-13

## PROJECT_STATUS.md 통계 드리프트 교정 — 문서

자율 루프 R88. `PROJECT_STATUS.md`는 "여기부터 읽는다"는 단일 진입 문서인데 §2.1 구성
표의 수치가 코드 실측과 벌어져 있었다(R52까지 마이그레이션이 늘고 컨트롤러/라우트가
추가됐는데 표는 옛날 값). 진입 문서가 틀리면 재개할 때 잘못된 전제로 시작한다.

### 교정 (전부 코드 정적 카운트로 검증)

| 항목 | 기존 | 실측(R88) | 근거 |
|---|---|---|---|
| BE 컨트롤러 | 28 | **33** | `grep -rl @RestController` |
| BE 엔드포인트 | ~78 | **~89** | `@(Get\|Post\|Put\|Patch\|Delete)Mapping` 카운트 |
| BE 마이그레이션 | V1~V24 | **V1~V28** | `db/migration/` 최신 = V28(구독 티어) |
| FE 라우트 | 14 | **16** | `app/**/page.tsx` |
| FE TS 라인 | 12,057 | **~14,200** | node_modules/.next 제외 wc -l |
| 기준일 | 2026-09-08 | **2026-09-13** | — |

APP(Flutter) Dart 7,614줄은 실측과 일치(변동 없음)라 유지.

### 남긴 것

- §2.3 데이터 규모(캔들 24만·신호 410·종목 476)는 **실가동 DB 스냅샷**이라 여기서
  재검증 불가 → 라벨 그대로 둠(이 세션에 백엔드/DB 미기동).
- FE "엔드포인트 사용 수"는 정확 카운트가 번거로워 근사치(~55→~60)로만 소폭 조정.

코드 변경 없음. 문서만.
</content>
