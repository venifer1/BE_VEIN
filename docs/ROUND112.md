# VEIN Round 112

Date: 2026-09-13

## 홈 관심종목에 라이브 등락률 표시 (FE, 실사용)

자율 루프 R112. 홈 "관심종목"이 현재가만 보여줘 오늘 오르는지/내리는지 한눈에 알 수
없었다. 크립토는 라이브 WS(`useLivePrice`)가 `change_rate`를 이미 주므로 **백엔드 변경 없이**
관심종목 행에 등락률을 표시.

### 바뀐 것 (프론트 전용)

- `WatchRow`: 크립토 항목에 라이브 등락률 `+X.XX%`(양수 초록·음수 빨강)를 가격 옆에 표시.
  라이브 값이 없으면(비크립토·미연결) 미표시(기존과 동일). BE/타입 무변경.

### 검증

`tsc`·`next build` 통과. 라이브 WS가 주는 change_rate 재사용(급등락 섹션과 동일 소스).
</content>
