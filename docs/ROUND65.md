# VEIN Round 65

Date: 2026-09-12

## 스캐너 결과 개수 + 필터 초기화 상시 노출 (FE) · 전략/백테스트 실측

자율 루프 R65. 전략(`/strategies`)·백테스트(`/backtests/run`)를 올바른 DTO로 실측: 유효 요청→200,
잘못된 파라미터(음수 target/stop, period 0, is_ratio>1, 알 수 없는 type)→400, 없는 리소스→404.
**전부 견고, 500 없음**(scanner R58 외 잔여 500 미발견).

버그가 없어, 스캐너 목록 UX를 다듬었다: 필터가 걸려 있어도 **"필터 초기화"가 빈 결과일 때만**
보여 결과가 있으면 필터를 한 번에 못 지웠다.

### 바뀐 것 (FE)

- 스캐너 신호 목록 상단에 **결과 개수**(`N개 신호`, 다음 페이지 있으면 `+`) + 필터 활성 시
  **"필터 초기화"** 버튼 상시 노출(기존엔 빈 상태에만 있었음).

### 검증

- 프론트 `typecheck`/`build` 통과. **Chrome smoke 0에러**. 백엔드/계약 무변경.
