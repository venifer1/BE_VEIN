# VEIN Round 119

Date: 2026-09-13

## PROJECT_STATUS §2.3 데이터 규모 실측 교정 (문서, R88 유형)

자율 루프 R119. §2.3 데이터 규모가 옛 스냅샷(캔들 24만·신호 410)이라, R90/R101에서 라이브
DB로 실제 확인한 값과 어긋났다. 검증된 값으로 교정(R88 통계 드리프트 교정과 같은 유형).

### 바뀐 것 (문서만)

- 캔들 24만 → **29.6만(295,840)**, 신호 410 → **316**. 둘 다 2026-09-13 R90/R101 라이브 DB
  실측값. 종목 수(476)는 이번에 재확인하지 않아 이전 값 유지(추측 변경 금지 — R107/R108 교훈).

### 검증

문서 정합성만. 캔들·신호 수치는 라이브 `select count(*)`로 확인한 값(R90/R101 로그). 코드 무변경.

_속보 목록/상세 CoinChips 딥링크, 알림 다이얼로그 친화 라벨 등은 이미 구현됨을 확인(추가 작업
불요)._
</content>
