# VEIN Round 69

Date: 2026-09-12

## 스캐너 "관심종목만" 빈 결과 전용 안내 (R61 후속, FE)

자율 루프 R69. R61이 스캐너에 "관심종목만" 토글을 넣었는데, 관심종목이 비었거나 그 종목에
신호가 없으면 일반 "필터 조건을 바꾸거나 초기화하세요"만 떠서 원인을 알기 어려웠다.

### 바뀐 것 (FE)

- 스캐너 빈 상태: `filter.watchlist_only`가 켜져 있고 결과가 없으면 **"관심종목 신호가 없습니다 /
  종목을 관심 등록하거나 필터를 끄세요"** 로 안내. 그 외는 기존 문구 유지.

### 검증

- 프론트 `typecheck`/`build` 통과. **Chrome smoke 0에러**. 백엔드/계약 무변경.

### R69 감사 메모(무수정 확인)

- 알림함 알림 행은 이미 `signal_id`가 있으면 신호 상세로 링크(갭 아님).
- 홈 "오늘의 주목 신호"는 `SignalCard`를 재사용 → R66 C목표 거리 표시가 자동 적용(일관).
