# VEIN Round 200

Date: 2026-09-13

## 전체 스위트 통합 헬스체크 + 세션 커버리지 총결산 (BE/FE, 마일스톤 R200)

자율 루프 R200(마일스톤). R180 이후 20라운드(R181~R199) 동안 백엔드 하위점수 분기·표기
헬퍼와 프론트 데모 상세 getter·생성기·다이제스트 테스트를 추가했으므로, 전체 스위트를
`--rerun-tasks`로 강제 재실행해 통합 통과를 확인.

### 결과

- 백엔드 전체 `gradlew test --rerun-tasks` (ASCII 경로) **BUILD SUCCESSFUL** — **테스트 파일
  50개 · @Test 230개** (R180 47파일·215 → **+3파일·+15**).
- 프론트 `vitest run` **8파일 132개** (R180 96 → **+36**, mockData/mockAdapter 상세 getter·
  생성기·다이제스트·경제캘린더).

### 세션 총결산 (R143 이전 → 현재)

- **백엔드**: 28파일·127 @Test → **50파일·230 @Test**(+22파일·+103).
- **프론트**: 47 → **132**(+85).
- 리팩터(동작 보존 추출) 4건: kimchi premiumPct(R165)·movers changePct(R174)·jwt
  resolveBearerToken(R176) + 기타. 그 외 전부 가시성 확장/테스트 전용으로 프로덕션 동작 무변경.

### 결론

핵심 순수 산출식(유통량·성과·변동성·거래량비율·프리미엄·등락률·멱등키·Pattern Score
하위점수)·정규화·보안 라우팅·표기 헬퍼, 그리고 프론트 데모 파이프라인 전반이 회귀 보호됨.
mockData/mockAdapter 순수부 사실상 전수 커버.
