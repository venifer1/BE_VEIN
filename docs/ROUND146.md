# VEIN Round 146

Date: 2026-09-13

## 데모 백테스트 지표 집계(승률·손익비·복리수익·MDD) 단위 테스트 (FE, 회귀 보호)

자율 루프 R146. 오프라인 데모(mock) 백테스트가 체결 목록에서 지표를 뽑는
`metricsFromTrades`(`lib/mockData.ts`)는 승률·평균수익·복리 총수익·손익비·최대낙폭(MDD)을
한 번에 계산하는 실질 로직인데 테스트가 없었다.

### 바뀐 것

- `lib/mockData.ts`: `metricsFromTrades`를 export(순수).
- `lib/mockData.test.ts` +4:
  - 대표 케이스 `[10,-5,20]` → 승률 66.7·평균 8.33·복리총 25.4·손익비 6.00·MDD -5.0·최고
    20.00·최저 -5.00 전 필드 검증.
  - 손실 없는 승리만 → 손익비 99 상한("99.00")·승률 100.0.
  - 빈 목록 → 0값·best/worst null.
  - NaN/null 처리: `"abc"`→NaN 제외, **`null`→`Number(null)===0`이라 0%로 계수**(스킵 아님).

### 관찰 (실제 동작 확정)

테스트 작성 중 `return_pct: null`이 제외될 것으로 가정했으나 `Number(null)===0`이라
**0% 체결(승)로 집계**됨을 확인 → 가정 대신 실측 동작으로 단언 고정. 실데이터 경로는
백엔드가 계산하므로 데모 표시에만 영향(무해). 로직 변경 없이 동작을 테스트로 못박음.

### 검증

FE `npx vitest run` **6파일 60개 그린**(R145 56 → +4).
